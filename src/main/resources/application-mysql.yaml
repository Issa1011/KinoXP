spring:
  application:
    name: KinoXP

  datasource:
    url: jdbc:mysql://localhost:3307/KinoXP
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: create
    defer-datasource-initialization: true

  sql:
    init:
      mode: always