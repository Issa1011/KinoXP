spring:
  datasource:
    url: jdbc:mysql://db:3306/KinoXP?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Europe/Copenhagen
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
  sql:
    init:
      mode: always